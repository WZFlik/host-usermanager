//没有了import和package
public class User {
    remote加一

    //删除toString
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    remote加二

    // 用户名
    private String userName;

    // 密码
    private String password;
    //加三


}
